#Analysin each and every step

def fun_challenge(arr):

  a = 10 #O(1)

  a = 50 + 3  #O(1)

  for i in range(len(arr)): #O(n)

    def another_func(): #O(n)

      stranger = True   #O(n) because this will run as many times as the input will be !!

      a += 1  #O(n)

  return a   #O(1)

 # BIG O(3 + 4n) == BIG O(n)
 
  # we will find that soon !!